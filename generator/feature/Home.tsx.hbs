import { Suspense } from "react";

import {{pascalCase name}} from "../components/{{pascalCase name}}";

const Home = () => {
  return (
    <Suspense>
      <{{pascalCase name}} />
    </Suspense>
  );
};

export default Home;
