import { Suspense } from "react";

import {{pascalCase name}} from "../components/{{pascalCase name}}";

const {{pascalCase name}}Home = () => {
  return (
    <Suspense>
      <{{pascalCase name}} />
    </Suspense>
  );
};

export default {{pascalCase name}}Home;
