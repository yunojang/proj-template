import { Route, Routes } from "react-router-dom";

import {{pascalCase name}} from "./Home";

export const {{pascalCase name}}Route = () => {
  return (
    <Routes>
      <Route path="/" element={<{{pascalCase name}} />} />
    </Routes>
  );
};
